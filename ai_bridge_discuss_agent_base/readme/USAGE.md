For using this agent, just start a conversation with an **AI Agent** in the same way it would be done with an user.
- An **AI Agent** will only respond automatically in one-on-one chats, and only if the other participant is not another **AI Agent**.
- In a chat group, agents can be prompted to respond by mentioning them using **@** followed by the agent's name.
- Example: @Sales AI Agent ...
